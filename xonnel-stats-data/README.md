# xonnel-stats-data

A python library: xonnel-stats-data

- data container storing stats used in all other submodules of xonnel-stats

## Install

LOCAL:
cd xonnel-stats-data
pip install -e .

PyPI:
pip install xonnel-stats-data
