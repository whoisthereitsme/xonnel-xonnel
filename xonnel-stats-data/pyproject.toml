[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "xonnel-stats-data"
version = "0.1.0"
description = """A python library: xonnel-stats-data

- data container storing stats used in all other submodules of xonnel-stats"""
authors = [{name = "Xonnel"}]
readme = "README.md"
requires-python = ">=3.14"

[tool.setuptools.packages.find]
where = ["."]
