# xonnel-load

A python library: xonnel-load

- loading of any file type to memory
    - as reasonable format (e.g. dict for json, ElementTree for xml, etc.)
    - as bytes

## Install

LOCAL:
cd xonnel-load
pip install -e .

PyPI:
pip install xonnel-load
